#include<iostream>
#include<fstream>
using namespace std;

int main(){
    ofstream fout("oi.txt");

    for(int i=0;i<3;i++){
        fout<<"Deu certo?"<<endl;
    }
    fout.close();
    return 0;
}

 ofstream temp("temp.txt");
        int aux = 5 + log10(nLinhasArquivo+1/n);
        nomeArquivo = new char[20];
        sprintf(nomeArquivo,"%d.txt",nLinhasArquivo/n);
        for(int i=0; i< nDispositivos;i++){
            string buffer;
            // criar uma função que abre arquivo e outra que lê arquivo

            
            while (true){
                if(nomeArquivo.eof()){
                    break;
                }    
            }
            
            
        }